<header class="header" >
  <%= link_to root_path do %>
  <div class="back-btn">戻る</div>
  <% end %>
    <div class="logo">
      <h1>遺品整理</h1>
    </div> 
</header>

<div class="personal">
    <div class="personal-title">
    <h2>遺品について入力する</h2>
    </div>
    <div class="content">
    <%= form_with(model: @user, local: true) do |form| %>
    <h3>遺品のこと(希望)</h3>
      <%= form.select :belongings, User.belongings.keys.map { |k| [t("enum.user.belongings.#{k}"), k] }, { include_blank: "指定なし" }, class: "mb-3 sm:mb-0 mr-9 border border-line-200 rounded-[3px] rounded-[3px] py-3 pl-6 w-52 h-12 font-bold" %>
      <%= form.submit "保存する", class: "custom-button", style: "display: block;" %>
    <% end %>
    </div>
</div>
